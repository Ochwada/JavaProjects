public class HonorsEligibilityChecker {

    public static boolean isEligibleForHonors(int score, boolean hasPerfectAttendance) {
        // Implement using logical operators
        return (score >= 60) && (hasPerfectAttendance);
    }
}
